import {setCarrito, addCarrito} from './compras_frigor.js';

/* btnForm.addEventListener('click', () =>{
    const pintarCards = data => {
        data.forEach((producto) => {
            formulario.innerHTML = `
            Producto = ${producto.title}
            Precio = ${producto.precio}
            Descriocion = ${producto.descrip}
    `})}
    
}) */

/* setCarrito = objeto => {
    console.log(objeto)
    const producto = {// Forma los campos del carrito
        id: objeto.querySelector('.btn-dark').dataset.id,
        title: objeto.querySelector('h5').textContent,
        precio: objeto.querySelector('p').textContent,
        cantidad: 1// Se agrega el valor de defecto en la primera compra del producto
    } 
}*/
console.log(addCarrito)
console.log(setCarrito)
console.log(setCarrito(objeto))
console.log(objeto)
console.log(setCarrito.objeto)
setCarrito()
setCarrito
objeto